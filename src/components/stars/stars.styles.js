import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    
    viewStars: {
        flexDirection: 'row'
    },
    textStars: {
        fontSize: 12,
        fontWeight: 'bold',
        marginLeft: 5,
        color: '#737373'
    },
});

export default styles;