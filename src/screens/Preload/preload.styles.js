import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    viewPreLoad: {
        backgroundColor: '#63C2D1',
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },

    activityIndicatorPreLoade: {
        marginTop: 50,
    },
    

});

export default styles;